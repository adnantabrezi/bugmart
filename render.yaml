services:
  - type: web
    name: bugmart-classroom-lab
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        value: bugmart_super_secret_classroom_key_2026
